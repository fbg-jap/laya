# Laya Engine — core dependencies (all platforms)

# Web framework
fastapi>=0.131.0
uvicorn>=0.41.0
uvloop>=0.22.1; sys_platform != "win32"
starlette>=0.52.1

# Database
aiosqlite>=0.22.1

# HTTP
httpx>=0.28.1
aiohttp>=3.13.5

# Email (SMTP)
aiosmtplib>=5.1.0

# LLM
litellm>=1.81.14
openai>=2.21.0
tiktoken>=0.12.0
# mcp 2.x is a breaking major (e.g. McpError -> MCPError, lowlevel Server API
# changes); laya/mcp/http_server.py targets 1.x. Unpinned, a fresh first-run
# install pulls 2.x and the engine crashes on import.
mcp>=1.0.0,<2

# Vector DB
chromadb>=1.5.1
onnxruntime>=1.18.0

# Structured logging
structlog>=25.5.0

# Config & security
python-dotenv>=1.2.1
keyring>=25.7.0
bcrypt>=5.0.0
PyYAML>=6.0.3

# Schema validation
pydantic>=2.12.5
jsonschema>=4.26.0

# IANA timezone database. The Windows-bundled CPython ships without the
# system tz database, so `zoneinfo.ZoneInfo("UTC")` (and any named zone)
# raises ZoneInfoNotFoundError unless this pure-Python package is present.
# Without it the budget endpoint 500s and the scheduler loop crashes on Windows.
tzdata>=2025.1

# Utilities
tqdm>=4.67.3
tenacity>=9.1.4
rich>=14.3.3
# psutil: cross-platform process introspection. On Windows we use it for the
# parent-watchdog (os.getppid() is unreliable across reparenting) and for
# finding stale engines holding our port (no lsof on Windows).
psutil>=6.0.0

# Build support — setuptools provides the `distutils` shim needed by
# node-gyp (used to compile n8n's isolated-vm native addon).
# Python 3.12+ removed distutils from stdlib (PEP 632).
setuptools>=80.0.0
